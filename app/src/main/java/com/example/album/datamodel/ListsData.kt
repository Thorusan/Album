package com.example.album.datamodel


class ListsData {
    companion object {
        var users: List<UserData.User> = emptyList()
        var albums: List<AlbumData> = emptyList()
        var photos: List<PhotoData> = emptyList()
    }

}